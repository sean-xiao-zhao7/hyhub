import { Divider } from "react-native-paper";

const MyDivider = () => {
    return (
        <Divider
            style={{
                backgroundColor: "transparent",
                paddingVertical: 5,
            }}
        />
    );
};

export default MyDivider;
